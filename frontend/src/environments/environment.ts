// Entorno de desarrollo de la v1. La API base vive aqui (9_frontend.md §2).
// En Docker el navigateor accede a la API por http://localhost:8036 (CORS habilitado).
export const environment = {
  apiUrl: 'http://localhost:8036'
};